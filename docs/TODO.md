<!-- CREATED: true -->

# TODO

## 仕様確認待ち

- Launch の `promptTemplate` は実装上は空文字固定。入力UIを持つかは未決定
- `Ctrl + P` の検索範囲と一致条件は単純な名前部分一致。高度検索にするかは未決定
- `ignored` 判定済みエントリの表示ルールは未整理

## 実装差分の整理

- 既存ドキュメントにある「コマンドテンプレート」は現行UIに存在しない
- 既存ドキュメントにある「history.jsonl 末尾1000行のみ集計」は現行実装と一致しない
- 既存ドキュメントにある token 推定式は現行実装と一致しない

## 将来拡張

- 右ペインの分割方向は実質2カラム運用。上下分割を正式対応するか要検討
- タブ状態の永続化が必要か要検討
- 大規模ディレクトリ向けの仮想描画や検索高速化は未対応
- 大容量テキスト/CSV/PDF の部分読込は未対応
- プレビュー対応形式の追加は未検討

## 性能

- `history.jsonl` を全件読込しているため、履歴肥大化時の負荷を要検証
- ツリーは全件描画のため、大量ノード時の描画負荷を要検証
- `git check-ignore` の呼び出し頻度とキャッシュ寿命は要検証
- HEIC変換キャッシュの破棄方針は未整理

## セキュリティ・運用

- `settings.json` の保存場所をアプリ配布形態ごとに見直すか要検討
- Terminal.app 起動コマンドの許容入力範囲は追加検証が必要
- ローカルファイル削除時にゴミ箱経由へ変更するかは未決定
